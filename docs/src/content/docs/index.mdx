---
title: Llooma
description: A (less) minimal LLM chat app that runs entirely in your browser.
template: splash
hero:
  tagline: A (less) minimal LLM chat app. Run it in your browser alone, or host it for your team.
  image:
    light: ../../assets/logo-mark.png
    dark: ../../assets/logo-mark-dark.png
  actions:
    - text: Install it
      link: /guides/installation/
      icon: right-arrow
    - text: See what it does
      link: /features/
      icon: document
      variant: secondary
    - text: Source on GitHub
      link: https://github.com/cedhuf/llooma
      icon: external
      variant: minimal
---

{/* Indentation inside CardGrid is load-bearing: indent a line by four spaces and MDX reads it as a code block, which is what broke these cards once. MDX files are excluded in .prettierignore for the same reason. Keep this comment on one line, since a wrapped one breaks the parse. */}

import { Image } from 'astro:assets';
import { Card, CardGrid } from '@astrojs/starlight/components';
import Credits from '../../components/Credits.astro';
import sections from '../../assets/screenshots/sections.png';
import themes from '../../assets/screenshots/themes.png';

{/* A div, not a p: MDX wraps multi-line children in a paragraph of its own, and a p inside a p is invalid: browsers split it and the pill loses its content. */}

<div class="fork-note">
	A fork of [Hollama](https://github.com/fmaclen/hollama) by
	[fmaclen](https://github.com/fmaclen)
</div>

:::caution[Early preview]
Llooma is not made for production use. Expect breaking changes, unfinished features and rough
edges. Recent work has focused on server mode; local mode may have inconsistencies that still
need a verification pass.
:::

<figure class="shot shot--bleed">
	<Image src={sections} alt="The sidebar, the Library, the Interface settings, and a conversation over a wallpaper" loading="eager" />
	<figcaption>Your conversations, your Library, your servers. Nothing of it leaves the machine unless you send it.</figcaption>
</figure>

<CardGrid>
<Card title="Two ways to run it" icon="laptop">
**Local** keeps everything in your browser, with your own provider keys. **Server** signs users in, stores their data per account, and keeps the API keys where a browser can never read them.

[Choose a mode →](/guides/running-modes/)
</Card>

<Card title="Bring your own models" icon="puzzle">
Ollama, OpenAI, Claude, Infomaniak and anything OpenAI-compatible. Several at once, each with its own label and colour, so you always know which endpoint is about to be billed.

[See the providers →](/reference/providers/)
</Card>

<Card title="Documents, read in the tab" icon="open-book">
A PDF, a spreadsheet, a Word file. Parsed in your browser and never uploaded, in server mode too. Optional OCR for scans, and a vision fallback when a page is a picture of words.

[How it works →](/features/documents/)
</Card>

<Card title="Conversations that keep fitting" icon="seti:clock">
`/compact` replaces a long history with a structured summary so it still fits in the window. Nothing is deleted, and one click puts it all back.

[Read about compaction →](/features/compaction/)
</Card>
</CardGrid>

<section class="band">

## Six themes, each with a light and a dark ramp

Classic, Dracula, Catppuccin, Gruvbox, Nord and Solarized. Pick a palette without committing to a
mode, or follow the system without committing to a palette.

<figure class="shot">
	<Image src={themes} alt="The six themes, light ramps on the top row and dark on the bottom" />
</figure>

</section>

## Built on

Llooma is a small amount of glue around a lot of other people's work.

<Credits />

<section class="band">

<p class="pill">Not sponsored</p>

## When local isn't an option

Running Ollama on your own machine means no third party at all, and that's what I'd recommend
first. It needs a decent GPU though, and no phone has one. If that's not realistic, someone else
runs the model, and it's worth caring who.

[Infomaniak](https://www.infomaniak.com/en/hosting/ai-tools) is the one I like: Swiss datacentres
they own and run, open-weight models, prompts they don't log or train on, and the GPU heat goes
into warming homes nearby. Any other OpenAI-compatible provider works just as well here.

</section>
